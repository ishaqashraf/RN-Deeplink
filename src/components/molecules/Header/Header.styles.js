import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  mainContainer: {
    alignItems: 'center',
    flexDirection: 'row',
    padding: 8,
  },
  title: {fontSize: 20},
  topIconContainer: {position: 'absolute', right: 16, top: 16},
});
